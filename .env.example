# Copy to .env.local and fill in real values. Do not commit .env files.

# Contact form endpoint — the API Gateway URL backed by Lambda + SES.
# Production value lives in .github/workflows/ci.yml (passed at build time).
# For local dev: paste the production URL or use a dev/staging stage URL.
VITE_CONTACT_API_ENDPOINT=https://vf95n20ew8.execute-api.us-east-1.amazonaws.com/contact
